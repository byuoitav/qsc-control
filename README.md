# qsc-control
consolidation of the QSC microservice code

For the video aspect of this microservice we rely on named components instead of named controls.  The audio component must end in _Gain for proper operation.
